[package]
name = "evalforge-cli"
version = "0.7.0"
edition = "2021"

[[bin]]
name = "evalforge"
path = "src/main.rs"

[dependencies]
evalforge-core = { path = "../evalforge-core" }
clap = { version = "4", features = ["derive"] }
serde_json = "1"
chrono = { version = "0.4", features = ["serde"] }
