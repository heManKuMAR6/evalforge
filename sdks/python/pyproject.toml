[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "evalforge"
version = "0.2.0"
description = "Framework-agnostic LLM agent evaluation harness"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = []

[project.urls]
Homepage = "https://github.com/heManKuMAR6/evalforge"

[tool.maturin]
binaries = ["evalforge"]
manifest-path = "../../crates/evalforge-cli/Cargo.toml"
python-source = "."
strip = true

[project.optional-dependencies]
dev = ["pytest>=7.0"]
